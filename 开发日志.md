# 小灰桌宠 - 开发日志

## 📅 项目概览

| 项目 | 详情 |
|------|------|
| 项目名称 | 小灰桌宠 |
| 开发日期 | 2026年7月 |
| 技术栈 | Python 3.12 + PyQt5 |
| 运行环境 | Windows 10/11 |
| 开发者 | Trae AI Agent |

---

## 🔄 版本历史

### v1.0 - 初始版本
- ✅ 基础窗口框架（无边框、置顶、透明背景）
- ✅ 角色图片显示
- ✅ 鼠标拖拽移动
- ✅ 左键点击说话 + 弹跳动画
- ✅ 双击摸头动画
- ✅ 右键菜单（说话/跳一跳/摸摸头/置顶切换/关闭）
- ✅ 自动说话（30~80秒随机触发）
- ✅ 外发光光晕效果

### v2.0 - 功能增强
- ✅ 支持两张图片循环播放动画（说话时 gray1 ↔ gray2）
- ✅ 自动裁剪图片透明边缘
- ✅ 统一边界框对齐多张图片
- ✅ 自适应图片比例（避免压缩变形）
- ✅ 可调整角色尺寸比例
- ✅ 去除外发光光晕
- ✅ 台词面向大众优化（保留"主人"称呼和暧昧语气）
- ✅ 接入 DeepSeek API 聊天功能
- ✅ 聊天输入框集成在桌宠脚下
- ✅ 聊天回复显示在头顶气泡内
- ✅ API 密钥通过 GUI 对话框配置
- ✅ 自动说话间隔调整为 5~10 分钟

### v2.1 - 架构重构
- ✅ **模块化拆分** — 单文件 720 行 → 7 个模块文件
- ✅ **pet_window.py** — 主窗口（绘制、交互、动画）
- ✅ **chat.py** — 聊天模块（ChatHandler + ConfigDialog）
- ✅ **bubble.py** — 气泡组件（独立为控件）
- ✅ **dialogues.py** — 台词数据（LINES / PET_REPLIES）
- ✅ **config_manager.py** — 配置读写（load_config / save_config）
- ✅ **deepseek_worker.py** — 后台 LLM 线程
- ✅ **main.py** — 整洁的入口文件
- ✅ 界面紧凑优化（缩小名字、状态、聊天框间距）
- ✅ 移除 "陪伴中" 文字和紫色状态点

### v2.2 - 功能扩展
- ✅ **番茄钟功能** — 5~60分钟可调节，专注时触摸有专属台词，自动休息
- ✅ **聊天记录** — 自动保存对话历史，最多保留50条
- ✅ **触摸分区** — 上半50%摸头、中间25%摸身体、下半25%摸腿，各有专属台词
- ✅ **拖拽不触发台词** — 纯点击才触发互动
- ✅ **pomodoro.py** — 独立番茄钟模块
- ✅ **path_helper.py** — 资源路径处理模块

### v2.3 - 菜单重构
- ✅ **菜单简化** — 合并为"聊天模式"、"功能模式"、"关闭桌宠"三个选项
- ✅ **聊天模式面板** — 包含说句话吧、聊天模式、聊天记录、摸摸头、配置API
- ✅ **功能模式面板** — 包含开始/暂停/停止番茄、设置时长、置顶切换、配置API
- ✅ **精美面板设计** — 毛玻璃效果、圆角边框、悬停动画
- ✅ **demo.html** — 完整功能演示页面（含桌宠拖拽、右键菜单、番茄钟、聊天）

### v3.0 - 好感度系统
- ✅ **五级好感度** — 0~100区间，五颗心实时显示（灰心→红心渐变）
- ✅ **好感度联动行为** — 完成番茄+1、正常聊天+1、辱骂-5、中断番茄-3
- ✅ **分层台词系统** — 低/中/高三套触摸台词（摸头/摸身体/摸腿各三套）
- ✅ **好感度影响AI语气** — system prompt 根据好感度动态生成（低：冷淡催促 / 高：温柔深情）
- ✅ **辱骂检测** — LLM判断是否辱骂，辱骂触发好感度下降
- ✅ **好感度变化提示** — 好感度升降时小灰会说对应台词
- ✅ **手动修改好感度** — 功能面板增加"好感度修改（仅供测试）"按钮（±10）
- ✅ **好感度持久化** — 保存到 config.json，重启不丢失

### v3.1 - 智能主动说话
- ✅ **频率可调** — 关闭/低/中/高四档，聊天模式面板可切换
- ✅ **健康提醒导向** — 主动说话主要提醒喝水、起身锻炼、眼部休息、坐姿纠正
- ✅ **好感度影响提醒语气** — 低好感度命令式、中好感度朋友式、高好感度撒娇式
- ✅ **API调用主动说话** — 配置API后主动说话调用LLM生成，未配置则使用内置台词
- ✅ **auto_talk_level持久化** — 保存到 config.json

### v3.2 - 多API支持
- ✅ **三种API预设** — DeepSeek / 豆包 / ChatGPT 一键切换
- ✅ **自定义API** — 支持手动输入任意兼容OpenAI格式的API地址和模型
- ✅ **配置界面优化** — 预设按钮高亮选中、URL和模型名独立输入框
- ✅ **demo.html同步更新** — HTML版同步支持多API预设

### v3.3 - UI优化与修复
- ✅ **番茄钟进度条** — 计时条增加红黄渐变进度条，实时显示剩余比例
- ✅ **气泡滚动支持** — 移除 pointer-events:none，添加自定义滚动条样式
- ✅ **气泡尺寸增大** — max-width 220→280px，max-height 160→220px
- ✅ **番茄钟布局调整** — 改为垂直布局，进度条显示在时间下方
- ✅ **PyQt5 QHBoxLayout修复** — ChatPanel/FuncPanel 补充 QHBoxLayout 导入

---

## 🐛 Bug 修复记录

| 时间 | 问题 | 原因 | 解决方案 |
|------|------|------|----------|
| 7/1 | .bat 文件无法启动 | UTF-8 编码导致中文乱码 | 使用 GBK 编码重写 bat 文件 |
| 7/1 | 图片背景去除导致主体失真 | 简单颜色距离判断误判主体颜色 | 改用泛洪填充法，仅去除与边缘相连的背景 |
| 7/2 | gray1/gray2 切换时位置不一致 | 两张图片裁剪后的尺寸不同 | 使用统一边界框裁剪所有图片 |
| 7/2 | 图片被上下压缩 | 使用固定正方形尺寸缩放 | 改为自适应窗口空间，保持宽高比 |
| 7/2 | 程序启动后立即退出 | max_width/max_height 是浮点数 | 转换为 int 类型 |
| 7/2 | 配置保存后未生效 | 主窗口 self.config 未重新加载 | accept 后调用 load_config() 刷新 |
| 7/10 | 中文按钮文字显示不全 | QPushButton高度和padding不足 | padding 增至22px，添加 min-height:50px |
| 7/10 | 对话气泡和番茄钟重叠 | 绝对定位冲突 | 聊天模式时隐藏番茄钟，移到角色右侧 |
| 7/10 | API调用导致界面冻结 | 主线程阻塞 | 使用QThread后台调用 |
| 7/11 | QHBoxLayout未定义 | ChatPanel缺少QHBoxLayout导入 | 补充 `from PyQt5.QtWidgets import QHBoxLayout` |
| 7/11 | 聊天气泡长文本无法滚动 | pointer-events:none + 无滚动条 | 移除限制，添加自定义webkit-scrollbar |
| 7/12 | 番茄钟计时条不见了 | 缺少进度条HTML和CSS | 添加 pomo-progress-bar 结构和样式 |
| 7/12 | 多API切换不支持 | 代码硬编码DeepSeek地址 | 添加 API_PRESETS 配置和预设按钮 |

---

## 📁 文件结构

```
桌宠/
├── main.py              # 入口文件（启动应用）
├── pet_window.py         # 主窗口（绘制、交互、动画、好感度系统）
├── chat.py               # 聊天模块（API调用 + 好感度system prompt生成 + 辱骂检测）
├── bubble.py             # 气泡组件
├── dialogues.py          # 分层台词数据（低/中/高好感度各三套）
├── config_manager.py     # 配置读写（含 affection / auto_talk_level）
├── deepseek_worker.py    # 后台 LLM 线程（支持自定义 temperature）
├── pomodoro.py           # 番茄钟模块
├── path_helper.py        # 资源路径处理模块
│
├── demo.html             # HTML完整演示页面（含多API预设、好感度、番茄钟进度条）
├── gray1.png             # 角色图片1（默认）
├── gray2.png             # 角色图片2（说话动画切换）
│
├── config.json           # 配置文件（API密钥、好感度、番茄时长、主动说话频率）
├── chat_history.json     # 聊天记录（自动生成）
├── 开发日志.md            # 本文件
├── 初赛Demo作品帖.md      # TRAE大赛参赛帖
├── 报名帖_学习工作_星绊桌宠.md  # 报名创意提案
│
├── dist/
│   ├── little gray.exe   # 打包后的可执行文件
│   ├── config.json       # 运行时配置
│   └── chat_history.json # 运行时聊天记录
└── __pycache__/          # Python缓存
```

---

## 🎮 功能说明

### 交互方式
- **左键点击**：说话 + 弹跳 + 图片动画
- **双击**：摸头模式（左右晃动）
- **拖拽**：移动位置
- **右键**：菜单操作

### 菜单选项
| 选项 | 功能 |
|------|------|
| 💬 聊天模式 | 打开聊天模式面板 |
| ⚙️ 功能模式 | 打开功能模式面板 |
| ✕ 关闭小灰 | 温柔关闭（说晚安后退出） |

#### 聊天模式面板
| 选项 | 功能 |
|------|------|
| 💬 说句话吧 | 随机触发一句台词 |
| 💬 聊天模式 | 显示/隐藏聊天输入框（角色脚下） |
| 📋 聊天记录 | 查看对话历史 |
| 🤚 摸摸头 | 触发摸头动画 |
| ⚙️ 配置API | 配置 API（支持DeepSeek/豆包/ChatGPT三种预设 + 自定义） |
| 🔔 主动说话频率 | 关闭/低/中/高 四档可切换 |

#### 功能模式面板
| 选项 | 功能 |
|------|------|
| 🍅 开始番茄 | 启动番茄钟计时 |
| ⏸ 暂停番茄 | 暂停当前番茄 |
| ⏹ 停止番茄 | 停止番茄钟 |
| ⏱ 设置时长 | 设置番茄钟时长（5~60分钟） |
| 📌 置顶切换 | 切换窗口是否置顶 |
| ⚙️ 配置API | 配置 API（同上） |
| ❤️ 好感度修改 | 手动±10调整好感度（仅供测试） |

### 好感度系统
| 行为 | 好感度变化 |
|------|-----------|
| 完成番茄钟 | +1 |
| 正常聊天 | +1 |
| 辱骂小灰（LLM判断） | -5 |
| 中断番茄钟 | -3 |

- **低好感度（0~33）**：冷淡、不耐烦、催促学习
- **中好感度（34~66）**：温暖亲切、朋友般关心
- **高好感度（67~100）**：温柔深情、撒娇、多用颜文字

### 自动行为
- 主动说话频率可调（关闭/低/中/高），主要提醒喝水和起身锻炼
- 主动说话语气受好感度影响
- 呼吸浮动效果（持续）
- 说话时 gray1/gray2 循环切换动画

---

## ⚙️ 配置说明

### 角色尺寸
在 `pet_window.py` 中调整缩放比例：
```python
max_width = int((self.W - 40) * 0.55)  # 当前 55%
max_height = int((self.H - 80) * 0.55)
```
- `0.7` = 较大
- `0.55` = 适中（当前）
- `0.4` = 较小

### 台词修改
修改 `dialogues.py` 中的 `LINES` 和 `PET_REPLIES` 列表。

### 图片更换
替换 `gray1.png` 和 `gray2.png` 即可更换角色形象。

### 聊天 API 配置
1. 右键小灰 → 聊天模式 / 功能模式 → ⚙️ 配置API
2. 选择API服务预设（DeepSeek / 豆包 / ChatGPT / 自定义）
3. 输入API密钥、确认API地址和模型名称
4. 保存后即可在"聊天模式"中对话

### 好感度配置
- 初始好感度在 `config.json` 中的 `affection` 字段设置（默认40）
- 功能面板可手动±10调整（仅供测试）
- 好感度变化自动保存

### 主动说话配置
- `config.json` 中的 `auto_talk_level` 字段（0=关, 1=低, 2=中, 3=高）
- 聊天模式面板可实时切换频率

---

## 📦 打包说明

```bash
# 使用 PyInstaller 打包（注意入口改为 main.py）
python -m PyInstaller --onefile --windowed --name "XiaoHui" --add-data "gray1.png;." --add-data "gray2.png;." main.py

# 打包后文件位置
# dist/XiaoHui.exe
```

---

## 📝 待办事项

- [ ] 增加更多角色表情帧
- [ ] 增加计划功能以及部分agent功能
- [ ] 支持自定义角色图片路径
- [ ] 增加设置界面（尺寸、语速、自动说话间隔）
- [ ] 支持多显示器定位
- [ ] 添加任务栏托盘图标
- [ ] 天气/时间等桌面小部件
